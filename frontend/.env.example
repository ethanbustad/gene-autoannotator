# Browser API calls go through the same-origin /api/backend proxy.
# Override server/proxy backend calls only if FastAPI runs elsewhere.
BACKEND_API_BASE_URL=http://127.0.0.1:8000

# Next.js server routes use MongoDB directly for annotation search/review.
MONGO_URI=mongodb://localhost:27017/gene_autoannotator
